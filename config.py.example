# Database Configuration
# Replace with your actual Neon PostgreSQL URI or any PostgreSQL connection string
NEON_URI = "postgresql://username:password@host:port/dbname?sslmode=require"

# API Configuration
API_HOST = "0.0.0.0"
API_PORT = 8000
DEBUG_MODE = True

# Scraper Configuration
# How often to run the full scraping cycle (in hours)
SCRAPE_INTERVAL = 24  

# Stores to scrape (True to enable, False to disable)
STORES = {
    "tunisianet": True,
    "mytek": True,
    "Wiki": True,
    "jumia": True,
    "omega": True,
    # Add more stores as needed
}

# Proxy configuration (if needed)
USE_PROXY = False
PROXY_URL = ""  # Example: "http://user:pass@host:port"

# Logging configuration
LOG_LEVEL = "INFO"  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE = "barbechli.log" 